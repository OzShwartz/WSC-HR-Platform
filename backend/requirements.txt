pandas>=2.2
pydantic>=2.6
python-dotenv>=1.0

# Optional — only needed if you set OPENAI_API_KEY or ANTHROPIC_API_KEY to
# enable real LLM calls in the AI layer (backend/ai/). Without a key, the
# pipeline runs fully using deterministic fallback narratives. Install with:
#   pip install openai        # if using OPENAI_API_KEY
#   pip install anthropic     # if using ANTHROPIC_API_KEY

# API layer (serves the React app in src/)
fastapi>=0.110
uvicorn>=0.29

# Tests
pytest>=8.0
